import { SearchCandidatePipe } from './search-candidate.pipe';

describe('SearchCandidatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCandidatePipe();
    expect(pipe).toBeTruthy();
  });
});
